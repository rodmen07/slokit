version: "prometheus/v1"
service: automation-service
labels:
  owner: platform
  tier: platform
slos:
  - name: requests-availability
    objective: 99.5
    period: 30d
    description: "99.5% of HTTP requests to automation-service succeed (non-5xx)"
    sli:
      events:
        error_query: sum(rate(http_request_duration_seconds_count{job="automation-service",code=~"5.."}[{{.window}}]))
        total_query: sum(rate(http_request_duration_seconds_count{job="automation-service"}[{{.window}}]))
    alerting:
      name: automation-service-high-error-rate
      annotations:
        runbook: https://github.com/rodmen07/microservices/blob/main/monitoring/RUNBOOK.md#automation-service
      page_alert:
        labels:
          severity: page
          service: automation-service
      ticket_alert:
        labels:
          severity: ticket
          service: automation-service
  - name: requests-latency
    objective: 99.0
    period: 30d
    description: "99.0% of automation-service requests are served under 500ms"
    sli:
      latency:
        histogram_metric: http_request_duration_seconds
        threshold: "0.5"
        selector: job="automation-service"
    alerting:
      name: automation-service-high-latency
      annotations:
        runbook: https://github.com/rodmen07/microservices/blob/main/monitoring/RUNBOOK.md#automation-service
      page_alert:
        labels:
          severity: page
          service: automation-service
      ticket_alert:
        labels:
          severity: ticket
          service: automation-service
